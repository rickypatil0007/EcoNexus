/**
 * Toaster Component — EcoNexus
 *
 * Minimal toast notification component used by RootProviders.
 * Full implementation will use shadcn/ui Sonner in Phase 2+.
 *
 * Phase 1: Simple stub to prevent import errors.
 * Phase 2: Install and configure shadcn/ui Sonner (npx shadcn@latest add sonner).
 */

"use client";

export function Toaster() {
  // Phase 2: Replace with <Toaster /> from "sonner"
  // import { Toaster } from "sonner"
  return null;
}
